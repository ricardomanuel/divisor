group 'ch.ffhs.task.divisor'
version '1.0-SNAPSHOT'

apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
    flatDir {
        dirs 'libs'
    }
}

dependencies {
    compile name: "student"
}
